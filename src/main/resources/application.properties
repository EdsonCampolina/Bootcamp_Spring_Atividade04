spring.profiles.active=${APP_PROFILE:test}

spring.jpa.open-in-view=false

security.oauth2.client.client-id=${CLIENT_ID:security.oauth2.client.client-id}
security.oauth2.client.client-secret=${CLIENT_SECRET:security.oauth2.client.client-secret}

jwt.secret=${JWT_SECRET:MY-JWT-SECRET}
jwt.duration=${JWT_DURATION:86400}
